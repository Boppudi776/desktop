// Copying elements from one array to another using slice

// First parameter is from which index we have to copy

var h = [1,2,3,4,5];
console.log("h before slice",h);
var i = h.slice(2);
console.log("i after slice",i);
